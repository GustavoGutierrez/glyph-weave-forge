# Tasks: Bootstrap GlyphWeaveForge

## Phase 1: Crate Foundation
- [x] 1.1 Create `Cargo.toml` with crate metadata, `fs` default feature, optional `mermaid`/`math`, core deps (`thiserror`, `pulldown-cmark`, `serde`, `serde_json`), and lint/doc settings feasible for this repo.
- [x] 1.2 Create `src/lib.rs`, `src/api/mod.rs`, `src/core/mod.rs`, `src/pipeline/mod.rs`, and adapter module trees with public exports and crate-level docs.
- [x] 1.3 Implement `src/core/error.rs`, `src/core/document.rs`, and `src/core/ports.rs` for `ForgeError`, theme/page value objects, `Document` IR/fallback nodes, and core-owned ports.

## Phase 2: Public API and Pipeline Skeleton
- [x] 2.1 Implement `src/api/options.rs` with `MarkdownSource`, `OutputTarget`, `ConvertOptions`, `PdfOutput`, built-in theme/page/layout enums, and deterministic output naming helpers.
- [x] 2.2 Implement `src/api/forge.rs` fluent builder methods (`from_*`, `to_*`, `with_*`, `convert`) with validation that rejects incomplete requests and allows renderer/resolver swaps.
- [x] 2.3 Implement `src/pipeline/convert.rs` to normalize sources, call parser/resolver/render ports in order, collect fallback behavior, and return `Result<PdfOutput, ForgeError>`.
- [x] 2.4 Implement `src/pipeline/output.rs` for memory/file/directory delivery, derived filename rules, and no-partial-success behavior on write failures.

## Phase 3: Parsing and Resource Resolution
- [x] 3.1 Create `src/adapters/markdown.rs` to map supported CommonMark/GFM events into `Document`, preserving lists, quotes, links, emphasis, and fenced code language info.
- [x] 3.2 Add parser fallback handling for unsupported advanced markdown into visible `UnsupportedBlock` output instead of silent omission.
- [x] 3.3 Implement `src/adapters/resources.rs` filesystem resolution for path-relative assets and typed resource errors behind the resolver port.
- [x] 3.4 Implement closure-based caller resource resolution while keeping the core independent from filesystem concerns.

## Phase 4: Theme, Layout, and PDF Adapters
- [x] 4.1 Implement `src/adapters/render.rs` to render `Document` IR into PDF text lines with built-in theme/page options, code fences, image summaries, and visible missing-asset placeholders.
- [x] 4.2 Preserve the renderer as a core-owned port with a default minimal backend and public renderer swap support.
- [x] 4.3 Wire default adapters in `src/lib.rs` and `src/api/forge.rs` so default builds work with `fs` enabled and optional `mermaid`/`math` features only extend behavior.

## Phase 5: Tests, Docs, and Quality Gates
- [x] 5.1 Add unit tests for validation failures, output naming, markdown semantics, fallback behavior, and renderer-port swapping.
- [x] 5.2 Add integration tests in `tests/api_smoke.rs` and `tests/pdf_integration.rs` covering text/bytes/path input, memory/file/directory output, custom resolver images, and missing-resource placeholders.
- [x] 5.3 Add feature-gate regression tests for default vs optional markdown extensions, ensuring disabled features keep non-lossy fallback behavior.
- [x] 5.4 Write `README.md` usage guidance and re-run `cargo fmt --check`, `cargo clippy --all-targets --all-features -- -D warnings`, `cargo test`, `cargo test --all-features`, and `cargo doc --no-deps`.
